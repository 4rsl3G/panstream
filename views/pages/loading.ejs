<div class="max-w-3xl mx-auto px-6 py-20 text-center">
  <div class="tracking-[0.45em] uppercase text-xs opacity-60">Loading</div>
  <h1 class="mt-4 text-2xl md:text-4xl font-semibold">Sedang mengambil data…</h1>
  <p class="mt-4 opacity-70">Server sedang menunggu respons dari API. Halaman akan mencoba ulang otomatis.</p>

  <div class="mt-10 flex items-center justify-center gap-3">
    <div class="pan-spinner"></div>
    <div class="opacity-70">Please wait…</div>
  </div>

  <div class="mt-10 opacity-50 text-sm">
    Jika terlalu lama, coba refresh manual.
  </div>

  <script>
    setTimeout(() => location.reload(), <%= retryMs || 1200 %>);
  </script>
</div>
